<Window x:Class="Stability.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
                <GradientStop Color="White" Offset="1"/>
                <GradientStop Color="#FF8F8F8F"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="MenuRow" Height="20*" MaxHeight="20" MinHeight="20"/>
            <RowDefinition x:Name="RxDataRow" Height="241*" MinHeight="50"/>
            <RowDefinition x:Name="TxDataRow" Height="29*"/>
            <RowDefinition x:Name="ButsRow" Height="29*" MaxHeight="29" MinHeight="29"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Height="20" VerticalAlignment="Top">
            <MenuItem  Header="Connection">
                <MenuItem x:Name="Con" Header="Connect" Visibility="Collapsed"/>
                <MenuItem x:Name="Discon"  Header="Disconnect" Visibility="Collapsed"/>
                <MenuItem Header="Autoconnect" IsCheckable="True" IsChecked="True" Checked="MenuItem_OnChecked" Unchecked="MenuItem_OnUnchecked"/>
                <Separator/>
                <MenuItem Header="Exit" Click="OnExit"/>
            </MenuItem>
        </Menu>
        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="1" Margin="10">
            <TextBlock Margin="0" TextWrapping="Wrap" Text="TextBlock" Foreground="{x:Null}" Background="White"/>
        </Border>
        <Border Margin="10,0" Grid.Row="2" BorderBrush="Black" BorderThickness="1" >
            <TextBox TextWrapping="Wrap" Text="TextBox"/>
        </Border>
        <Button Content="Send" HorizontalAlignment="Left" Margin="10,5,0,0" Grid.Row="3" VerticalAlignment="Top" Width="75"/>

    </Grid>

</Window>
